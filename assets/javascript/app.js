//Declare my global variables
//set up calls for database
//set up code to find destination and train names via user input.
//get schedule info in militarty time.
//Write code that makes minutes the frequency.
//Calculate when the next train will arrive, relative to the current time.
//create code that ensures users from many different machines can view the same train times.